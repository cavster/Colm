package CourseWorkn;

import java.util.Comparator;

private static class Comparer implements Comparator{

    @Override
    public int compare(Ticket a, Ticket sp2) {
        return (a.getPiority() < sp2.getPiority() ) ? -1: (a.getPiority() > sp2.getPiority()) ? 1:0 ;
    }


